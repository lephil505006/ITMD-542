extends layout

block content
  h1= recipe.dishName
  .infoGroup
    p= 'Occasion: ' + recipe.occasion
    p= 'Serving(s): ' + recipe.serving
    p= recipe.description 
    p(class="instruction")= recipe.instruction 
  .recipe-actions
    .edit-actions
      a(href='/recipes') Show all recipes
      |
      a(href=`/recipes/${recipe.id}/delete`) Delete recipe
      |
      a(href=`/recipes/${recipe.id}/edit`) Edit recipe